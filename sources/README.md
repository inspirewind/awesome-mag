# Sources

This directory is intended for source-specific metadata and notes.

Recommended future pattern:

```text
sources/<slug>/
├── metadata.yaml
├── notes.md
└── download.md
```

Use this directory when a source has enough detail that it should not live only as a single bullet in the main `README.md`.
